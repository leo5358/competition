#include <iostream>
#include <vector>
#define fin ios::sync_with_stdio(0);cin.tie(0);
using namespace std;

int main() {
    fin
    int N;
    cin >> N;
    
    vector<int> A(N);
    for (int i = 0; i < N; i++) {
        cin >> A[i];
    }
    
    int ans=0;
    for()
    return 0;
}